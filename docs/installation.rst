============
Installation
============

At the command line::

    $ easy_install dsh-orderwrt-bug

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dsh-orderwrt-bug
    $ pip install dsh-orderwrt-bug